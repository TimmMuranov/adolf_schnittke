#include <string>

namespace logs {
    std::string start = 
    "Привет)\n"
    "Доступлые команды к вашим услугам";

    std::string commands =
    "/start" // стартовая комманда. Выводит стартовые слова
    "/whataweek" // отображает четность недели
    ;
}